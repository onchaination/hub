---
import Article from '../../views/Article.astro';
import { loadItems } from '../../lib/content';
export function getStaticPaths() {
  return loadItems().map((item) => ({
    params: { section: item.section, slug: item.id },
    props: { item },
  }));
}
---

<Article item={Astro.props.item} />
