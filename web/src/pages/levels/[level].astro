---
import Browse from '../../views/Browse.astro';
import { levels } from '../../lib/content';
export function getStaticPaths() {
  return levels.map((level) => ({ params: { level } }));
}
---

<Browse kind="levels" value={Astro.params.level!} />
